// program to check if a string starts with another string

const string = 'saffwan';

const toCheckString = 'skk';

if(string.startsWith(toCheckString)) {
    console.warn('The string starts with "sa".');
}
else {
    console.warn(`The string does not starts with "jbd".`);
}